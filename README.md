# GeoCheck 📍  
ระบบเช็คอินตามพิกัดตำแหน่ง (Geolocation Check-in) พัฒนาด้วย **React + TypeScript** และ **Google Gemini AI** พร้อมรองรับการใช้งานบน **Android (APK)**

---

## ✨ ฟีเจอร์หลัก (Features)

- 📍 **เช็คอินตามพิกัด**: ตรวจสอบตำแหน่งปัจจุบันเทียบกับจุดเช็คอินที่กำหนด  
- 📏 **ตรวจสอบระยะห่าง**: อนุญาตให้เช็คอินเฉพาะเมื่ออยู่ในระยะที่กำหนด (เช่น 200 เมตร)  
- 📸 **ถ่ายรูปยืนยัน**: ต้องถ่ายรูปประกอบการเช็คอินทุกครั้ง  
- 💬 **แจ้งเตือนเข้า Telegram**: ส่งข้อมูลการเช็คอิน (ชื่อ, เวลา, สถานที่, รูปถ่าย) เข้ากลุ่ม Telegram ทันที  
- ⏰ **ตรวจสอบเวลา**: กำหนดช่วงเวลาเช็คอิน (เช้า / บ่าย / เย็น)  
- 🔔 **Persistent Notification (Android)**: แสดงแถบแจ้งเตือนค้างไว้บน Notification Bar พร้อมเวลานับถอยหลังแบบ Real-time ขณะเช็คอิน  

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

- **Frontend**: React, Vite  
- **Language**: TypeScript  
- **Map**: Leaflet / React-Leaflet  
- **AI**: Google Generative AI (Gemini)  
- **Notification**: Telegram Webhook (Google Apps Script)  
- **Mobile Framework**: Capacitor (Android Native)
- **Development Tool**: Manus AI (ใช้ในการสร้างและ Build APK)  

---

## 📱 แอปพลิเคชันมือถือ (Android App)

โปรเจกต์นี้รองรับการใช้งานเป็นแอป Android (APK) โดยมีรายละเอียดดังนี้:

- 🎨 **Adaptive Icons**: ไอคอนแอปมาตรฐาน Android (พื้นหลังสีม่วง + หมุดสีขาว)  
- 🧩 **Foreground Service**: รองรับการทำงานเบื้องหลังเพื่อให้นับถอยหลังได้แม่นยำ  
- 🔐 **Signed Release**: ไฟล์ APK ถูกลงนามด้วยใบรับรองดิจิทัลเพื่อความปลอดภัย  

---

## 🚀 การติดตั้งและใช้งาน (Installation)

### 1) ติดตั้ง Dependencies
```bash
npm install
```

### 2) ตั้งค่า Environment Variables
สร้างไฟล์ `.env` ที่ root ของโปรเจกต์ แล้วใส่ค่าดังนี้:
```env
VITE_TELEGRAM_WEBHOOK_URL=
VITE_TELEGRAM_CHAT_ID=
```

### 3) รันโปรแกรม (Web)
```bash
npm run dev
```

---

## 📦 การ Build APK (Android)

### 1) Build โปรเจกต์ Web
```bash
npm run build
```

### 2) Sync ข้อมูลไปยัง Android
```bash
npx cap sync android
```

### 3) Build APK ด้วย Android Studio หรือ Gradle
```bash
cd android && ./gradlew assembleRelease
```

---

## ⚠️ ข้อควรระวัง

- ไฟล์ `.env` เก็บข้อมูลสำคัญ **ห้ามอัพโหลดขึ้น GitHub** (ถูกกันไว้ใน `.gitignore` แล้ว)  
- การใช้งานบนมือถือ ต้องอนุญาตสิทธิ์ **Location (GPS)** และ **Notifications** เพื่อให้ฟีเจอร์ทำงานได้ครบ  
- หากติดตั้ง APK แล้ว Google Play Protect แจ้งเตือน ให้กด **More Details** และ **Install anyway** (เนื่องจากเป็นแอปติดตั้งเองนอก Store)  
